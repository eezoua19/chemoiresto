VITE_API_URL=http://localhost:4000

# Clé publique VAPID (notifications push) - facultative, doit correspondre a
# VAPID_PUBLIC_KEY côté backend. Generer une paire : npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
